apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: k8sdemo
  labels:
    app: postgres
    app.kubernetes.io/part-of: k8sdemo
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
