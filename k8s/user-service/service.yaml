apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: k8sdemo
  labels:
    app: user-service
    app.kubernetes.io/part-of: k8sdemo
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: 3002
      targetPort: 3002
      protocol: TCP
      name: http
